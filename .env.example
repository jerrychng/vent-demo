# API base URL (used when mock is off). No trailing slash.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Set to "false" to use the real backend. Default is mock (no backend required).
# NEXT_PUBLIC_USE_MOCK_API=false
